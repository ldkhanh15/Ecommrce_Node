import db from '../models'

const getOverview=(req)=>{
    return new Promise(async(resolve, reject)=>{
        try {
                
        } catch (error) {
            reject(error)
        }
    })
}

module.exports ={
    getOverview
}